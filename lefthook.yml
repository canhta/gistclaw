pre-commit:
  jobs:
    - name: go-fast-checks
      glob:
        - "*.go"
        - "go.mod"
        - "go.sum"
      run: make precommit FILES="{staged_files}"
      stage_fixed: true

pre-push:
  jobs:
    - name: lint
      run: make lint
    - name: coverage
      run: make coverage
